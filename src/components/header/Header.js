import React from 'react';
import './_header.scss';

const Header = () => {
  return (
    <header>
      <span className="header-text">spli</span>
      <span className="header-text">tter</span>
    </header>
  );
};

export default Header;
